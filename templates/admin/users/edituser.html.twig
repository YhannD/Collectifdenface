{% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
    <main class="container">
        <h1>Modifier un utilisateur</h1>
        <div>
            <h5>Image de profil</h5>
            <img src="{{ asset('assets/uploads/profiles/' ~ users.image) }}" alt="Image" width="150">
        </div>
        {{ form(userForm) }}

        <a class="btn btn-outline-secondary" href="{{ path('app_admin_utilisateurs') }}">Retour à la liste</a>

        {{ include('admin/users/_delete_form.html.twig') }}
    </main>
{% endblock %}

