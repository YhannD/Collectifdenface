{% extends 'base.html.twig' %}

{% block title %}Les expositions{% endblock %}

{% block body %}
    <main class="container">
        <section class="row">
            <div class="col-12">
                <h1>Les expositions du Collectif d'en Face</h1>
            </div>

                {% for exhibition in exhibitions %}
                    {% include 'exhibitions/_card.html.twig' %}
                {% endfor %}

            <div class="mt-3">
                {{ knp_pagination_render(exhibitions) }}
            </div>
        </section>
    </main>
{% endblock %}
