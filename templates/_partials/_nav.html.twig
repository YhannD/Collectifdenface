<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">Le collectif d'en face</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{#{{ path('app_exhibitions_index') }}#}">Les expositions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{#{{ path('app_editions_index') }}#}">Les éditions</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                       aria-expanded="false">
                        Les salons d'artistes
                    </a>
                    <ul class="dropdown-menu">
{#                        <li><a class="dropdown-item" href="#}{#{{ path('app_main') }}#}{#">Les salons des artists</a></li>#}
{#                        <li>#}
{#                            <hr class="dropdown-divider">#}
{#                        </li>#}
                        <li><a class="dropdown-item" href="{#{{ path('app_artistes_index') }}#}">Les artistes</a></li>
                        <li><a class="dropdown-item" href="{#{{ path('app_medias_images') }}#}">Les réalisations</a></li>
                        <li><a class="dropdown-item" href="{#{{ path('app_medias_musics') }}#}">Les musiques</a></li>
                        <li><a class="dropdown-item" href="{#{{ path('app_medias_videos') }}#}">Les vidéos</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{#{{ path('app_medias_categories') }}#}">Les categories</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link " href="#">Mon profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_logout') }}">Me déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_register') }}">M'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_login') }}">Me connecter</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="{#{{ path('app_medias_crud_index') }}#}">CRUD</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>