{% extends 'base.html.twig' %}

{% block title %}Les artistes{% endblock %}

{% block body %}
    <main class="container">
        <section class="row">
            <div class="col-12">
                <h1>Les artistes du Collectif d'en Face</h1>
            </div>

            {% for user in users %}
                {% include 'artist/_card.html.twig' %}
            {% endfor %}

            <div class="mt-3">
                {{ knp_pagination_render(users) }}
                {{ knp_pagination_render(users,'_partials/twitter_bootstrap_v4_pagination.html.twig') }}
            </div>
        </section>
    </main>
{% endblock %}
